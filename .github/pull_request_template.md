## Summary

What changed and why.

## Checks

- [ ] `.venv/bin/python analysis/verify_dt10_artifacts.py` passes
- [ ] `bash scripts/build_prompt_ablation_package.sh` passes (if prompt-ablation code or results changed)
- [ ] Result data committed together with the code that produced it
