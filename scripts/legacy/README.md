# Legacy entry points (frozen)

Shell pipelines and the Makefile from the original manuscript submission
(`e2e_pipeline.sh`, `run_full_manuscript_pipeline.sh`, `run_hybrid_rf_all_models.sh`,
`run_apples_to_apples_variance.sh`). They call scripts in `analysis/legacy/` and
target retired splits; kept for provenance, not expected to run. Current entry
points are in `scripts/`.
